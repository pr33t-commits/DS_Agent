# Experiment configurations

Future model, SFT, DPO and generation configuration files belong here.
The current agent is configured through CLI flags; it does not load YAML files.
The shared rollout defaults are defined by `inference.generation.GenerationConfig`.
Add algorithm configuration files alongside their implementations so configuration
values are validated and actually used.
