# Evaluation layer

`evidence.py` validates report citations against successful tool executions.
This checks references, not factual correctness. Future exact-match metrics,
reward evaluation and benchmarks belong here and should be shared by all
generation backends.
