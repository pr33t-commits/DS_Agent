# Data layer

Existing CSV inputs remain here to preserve input paths. `datasets.py` defines
shared default locations. Add preprocessing and reviewed training-dataset
builders here when post-training is implemented. Raw data must remain unchanged.
